NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Exact comma-separated admin emails. Keep ADMIN_EMAILS server-only in production.
ADMIN_EMAILS=
# Used only for client-side navigation visibility; server authorization uses ADMIN_EMAILS.
NEXT_PUBLIC_ADMIN_EMAILS=
# Never expose this key with NEXT_PUBLIC_. Required only for server-side database backups.
SUPABASE_SERVICE_ROLE_KEY=
